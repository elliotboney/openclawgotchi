# OpenClawGotchi Dependencies
# Install: pip install -r requirements.txt

# Telegram Bot
python-telegram-bot[job-queue]>=20.0
discord.py>=2.3.0

# LLM Fallback (Gemini)
litellm>=1.81.7
openai>=1.0.0
requests>=2.31.0
python-dotenv>=1.0.0
pypdf>=5.0.0

# Web UI (pwnagotchi-style screen mirror + plugin pages + webhooks; opt-in via WEB_UI_ENABLED)
aiohttp>=3.9.0

# E-Ink Display
Pillow>=9.0.0
smbus2>=0.4.0

# Raspberry Pi GPIO (for E-Ink)
# Note: Only needed on actual Pi hardware
# RPi.GPIO
# spidev
